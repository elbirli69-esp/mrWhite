# Mr White rooms server
#
# Local:
#   cd server && npm install && npm run dev
#
# Railway: root directory = repo root, Dockerfile = server/Dockerfile
# (see railway.toml). Exposes WebSocket on PORT and GET /health.
